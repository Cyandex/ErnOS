---
summary: "CLI reference for `ernos uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `ernos uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
ernos uninstall
ernos uninstall --all --yes
ernos uninstall --dry-run
```
