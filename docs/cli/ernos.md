---
summary: "CLI reference for `ernos ernos` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `ernos ernos ...`
  - You need migration guidance to current commands
title: "ernos"
---

# `ernos ernos`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `ernos ernos qr` (same behavior as [`ernos qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `ernos ernos qr` -> `ernos qr`
